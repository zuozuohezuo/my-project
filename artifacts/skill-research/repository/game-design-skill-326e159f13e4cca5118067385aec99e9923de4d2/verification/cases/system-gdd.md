Use the installed game-design-skill. Read `verification/fixtures/system-context.md`.
Draft an implementable Lantern Heat system GDD with player fantasy, precise
rules, states and transitions, formulas with defined variables and ranges,
edge-case resolutions, dependencies, tuning knobs, feedback requirements, and
Given-When-Then acceptance criteria. Label sample values as proposals rather
than validated facts and keep architecture choices out of the GDD. Do not
write files. End with a `Sources Applied` section naming the internal
game-design-skill references actually used.
